<!-- largest bottlenecks (_not_ directly the size!):
1. my own energy
2. ‘running ahead’ - intentionally coding imperfect logic to see if things fit, then going back to fix it - and debugging game loops in the browser is super annoying

disclaimers:
- ex-senior google dev (which i say only to establish baseline credibility) with a game design degree, but my life path took me in a direction such that i hadn’t actually fully programmed a game until now
- being my first game ever, i used training wheels to learn the ropes (AI, but really just like “how do you do X”-type prompts) but by the end I was coding everything by hand. there was some use at the end as well as the deadline approached (bugs, small features - nothing that eroded my own understanding). see WIP LLM policy.

technique categories:
1. “code golfing” - tuples+property mangling, inlining things (…intentional spaghetti code 😭), dirty JS tricks
2. Imperfect Abstractions - “forced” DRYness and messy side effects in pursuit of forcing consistency (doTimes)
3. actually concise systems - 3D lathe + concat, quantization and data tables (TBI)

post competition action items:
1. I still don’t fully understand rotation lmao but maybe i don’t have to
2. open deno proposal or PR for mangling props (doesn’t exist!)
3. W3C proposal for console append-only “live group” to make debugging loops in the browser easier (doesn’t exist!)
4. maybe directors cut - would want to keep it very close to size limit still -->

# Retrospective

TODO
